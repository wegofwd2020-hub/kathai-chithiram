# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Environment / secrets
.env
*.key
secrets/

# Generated media artifacts (do not commit rendered output by default)
*.mp4
*.mov
*.gif
*.wav
*.mp3
renders/
out/
tmp/

# Personal story data — NEVER commit
stories/
story_data/
*.story.json
# Default StoryArtifactStore location (holds story.txt / scene scripts / consent
# records). Runtime data, regenerable — never commit; a real story must not land
# in git even by accident.
kc_store/

# Test-persona real-inbox override (owner's machine only)
tests/kathai_chithiram/people/personas.local.json

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code plugin/skill cache (local tooling, not project code)
.agents/
skills-lock.json

# Subagent-driven-development scratch (ledgers, briefs, review diffs) — local only
.superpowers/

# AI tool configs (stray, not part of project)
opencode.jsonc
opencode.jasonc
.cursor/
.windsurf/
.clinerules/
.opencode/
.github/copilot-instructions.md
